const state={token:localStorage.getItem('mh_token'),data:null,analytics:null,recommendations:[],customers:null,content:[],calendar:[],integrations:[],route:location.hash.slice(1)||'dashboard'};
const app=document.getElementById('app'),title=document.getElementById('pageTitle'),toast=document.getElementById('toast');
const api=async(path,options={})=>{const headers={'Content-Type':'application/json',...(options.headers||{})};if(state.token)headers.Authorization=`Bearer ${state.token}`;const r=await fetch(path,{...options,headers});const j=await r.json().catch(()=>({}));if(r.status===401){state.token=null;localStorage.removeItem('mh_token');showLogin();throw Error('AUTH_REQUIRED')}if(!r.ok)throw Error(j.error||`Error ${r.status}`);return j};
const money=n=>new Intl.NumberFormat('es-AR',{maximumFractionDigits:0}).format(n);const showToast=m=>{toast.textContent=m;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)};
function setActive(route){document.querySelectorAll('[data-route]').forEach(a=>a.classList.toggle('active',a.dataset.route===route));document.getElementById('sidebar').classList.remove('open')}
function showLogin(){app.innerHTML=`<section class="login-card"><div class="brand"><span class="brand-mark">M</span><b>Marketing Hub</b></div><h2>Entrá a tu departamento de marketing</h2><p class="muted">Usá el acceso demo para probar el producto completo.</p><form id="loginForm" class="form-grid"><label>Email<input name="email" type="email" placeholder="tu@email.com" required></label><label>Contraseña<input name="password" type="password" minlength="4" required></label><button class="primary-btn">Ingresar</button></form><button id="demoLogin" class="secondary-btn full">Entrar con La Esquina demo</button></section>`;document.getElementById('loginForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);try{const r=await api('/api/auth/login',{method:'POST',body:JSON.stringify({email:f.get('email'),password:f.get('password')})});state.token=r.token;localStorage.setItem('mh_token',r.token);renderRoute()}catch(err){showToast(err.message)}};document.getElementById('demoLogin').onclick=async()=>{const r=await api('/api/auth/demo',{method:'POST',body:'{}'});state.token=r.token;localStorage.setItem('mh_token',r.token);renderRoute()}}
function renderDashboard(){const d=state.data,k=d.kpis;return `<div class="intro"><h2>¿Cómo está tu negocio hoy?</h2><p>Una lectura ejecutiva de lo importante y las acciones que pueden mover el negocio.</p></div><section class="status good"><div><strong>La tendencia general es positiva</strong><p class="muted">Ventas +12%, clientes +8% y leads +15%. Hay una oportunidad clara de eficiencia en publicidad.</p></div><button class="link-btn" onclick="location.hash='recommendations'">Ver prioridades</button></section><div class="section-head"><h3>Indicadores principales</h3><p>Comparación con el período anterior.</p></div><div class="kpi-grid">${k.map(x=>`<div class="kpi"><span class="kpi-label">${x.label}</span><div class="kpi-value">${x.value}</div><span class="trend ${x.trend}">${x.trend==='up'?'↑':'↓'} ${x.delta}</span></div>`).join('')}</div><div class="split"><div class="panel"><div class="section-head"><h3>Lo que está funcionando</h3></div><div class="insight"><strong>“Cumpleaños La Esquina” está generando ventas.</strong><span class="muted">29 ventas con $18.000 de inversión.</span></div><div class="insight"><strong>El contenido detrás de cocina tiene la mejor señal.</strong><span class="muted">18.400 de alcance y 1.120 interacciones.</span></div></div><div class="priority"><span class="priority-tag">Prioridad de hoy</span><h3>Revisar “Promo viernes”</h3><p>Gastó $27.500 y no generó ventas.</p><button class="primary-btn" onclick="location.hash='recommendations'">Ejecutar recomendación</button></div></div>`}
function renderAnalytics(){return `<div class="intro"><h2>Entendé qué está pasando y por qué.</h2><p>Analytics conecta métricas con explicaciones y oportunidades de negocio.</p></div><div class="tabs"><button class="tab active" data-tab="sales">Ventas</button><button class="tab" data-tab="marketing">Marketing</button><button class="tab" data-tab="customers">Clientes</button><button class="tab" data-tab="social">Redes sociales</button></div><div id="analyticsBody"></div>`}
function renderAnalyticsBody(tab='sales'){const d=state.analytics;const bodies={sales:`<div class="two-col"><div class="panel"><div class="metric-line"><b>Ventas</b><strong>$${money(d.sales.current)}</strong><span class="trend up">↑ 12%</span></div><div class="metric-line"><b>Clientes nuevos</b><strong>${d.sales.newCustomers}</strong><span class="trend up">↑ 8%</span></div><div class="metric-line"><b>Clientes recurrentes</b><strong>${d.sales.recurrentCustomers}</strong><span class="trend up">↑ 9%</span></div><div class="metric-line"><b>Conversión</b><strong>${d.sales.conversion}%</strong><span class="trend up">↑ 3 pp</span></div><div class="metric-line"><b>Ticket promedio</b><strong>$${money(d.sales.averageTicket)}</strong><span class="subtext">estimado</span></div><div class="chart"><span class="bar" style="height:48%"></span><span class="bar" style="height:58%"></span><span class="bar" style="height:52%"></span><span class="bar" style="height:66%"></span><span class="bar" style="height:62%"></span><span class="bar" style="height:78%"></span><span class="bar" style="height:88%"></span></div></div><div class="panel"><h3>¿Qué significa?</h3><p class="muted">La conversión muestra qué proporción de consultas termina en una compra. El 27% indica una mejora respecto del período anterior.</p><details class="explain"><summary>Ver explicación completa</summary><p><b>Qué medimos:</b> consultas que terminan en venta.</p><p><b>Qué pasa:</b> la conversión subió 3 puntos.</p><p><b>Por qué importa:</b> más oportunidades terminan en compra.</p></details></div></div>`,marketing:`<div class="panel"><div class="metric-line"><b>Inversión</b><strong>$${money(d.marketing.spend)}</strong><span class="trend down">↓ 4%</span></div><div class="metric-line"><b>Leads</b><strong>${d.marketing.leads}</strong><span class="trend up">↑ 15%</span></div><div class="metric-line"><b>Costo por lead</b><strong>$${money(d.marketing.cpl)}</strong><span class="subtext">promedio</span></div><div class="section-head"><h3>Rendimiento por campaña</h3></div>${d.marketing.campaigns.map(c=>campaign(c)).join('')}</div>`,customers:`<div class="segment-grid">${[['Nuevos',d.customers.new],['Recurrentes',d.customers.recurrent],['Inactivos',d.customers.inactive],['Potenciales',d.customers.potential]].map(([l,v])=>`<div class="segment"><span class="muted">${l}</span><strong>${v}</strong></div>`).join('')}</div><div class="panel" style="margin-top:16px"><h3>Oportunidad detectada</h3><p>Hay ${d.customers.inactive} clientes inactivos identificados.</p><button class="primary-btn" onclick="location.hash='customers'">Ver clientes</button></div>`,social:`<div class="two-col"><div class="panel"><div class="metric-line"><b>Alcance</b><strong>${money(d.social.reach)}</strong><span class="trend up">↑ 11%</span></div><div class="metric-line"><b>Interacciones</b><strong>${money(d.social.interactions)}</strong><span class="trend up">↑ 14%</span></div><div class="metric-line"><b>Seguidores</b><strong>${money(d.social.followers)}</strong><span class="trend up">↑ 6,2%</span></div><div class="metric-line"><b>Engagement</b><strong>${d.social.engagement}%</strong><span class="trend up">↑</span></div></div><div class="panel"><h3>Lectura</h3><p class="muted">Las interacciones crecen más rápido que la audiencia. El siguiente paso es medir cuántas interacciones terminan en consultas o ventas.</p></div></div>`};document.getElementById('analyticsBody').innerHTML=bodies[tab];document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-tab]').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderAnalyticsBody(b.dataset.tab)})}
function campaign(c){return `<div class="campaign ${c.performance}"><strong>${c.name}</strong><span>$${money(c.spend)}</span><span>${c.leads} leads</span><span>${c.sales} ventas</span><span class="state">${c.status}</span></div>`}
function renderRecommendations(){return `<div class="intro"><h2>Esto es lo que deberías hacer.</h2><p>Cada recomendación conecta evidencia con una acción concreta.</p></div><div class="reco-list">${state.recommendations.map(r=>`<article class="recommendation"><span class="priority-pill ${r.priority.toLowerCase()}">Prioridad ${r.priority}</span><h3 class="reco-title">${r.title}</h3><div class="reco-flow"><div class="flow-block"><h4>Dato</h4><p>${r.data}<br>${r.detected}</p></div><div class="flow-block"><h4>Análisis</h4><p>${r.analysis}</p></div><div class="flow-block"><h4>Conclusión</h4><p>${r.conclusion}</p></div><div class="flow-block"><h4>Recomendación</h4><p>${r.recommendation}</p></div></div><div class="action-box"><b>Acción:</b> ${r.action} <button class="primary-btn" style="margin-left:10px" onclick="executeReco('${r.id}')">Ejecutar</button></div></article>`).join('')}</div>`}
async function executeReco(id){await api(`/api/recommendations/${id}/action`,{method:'POST',body:'{}'});showToast('Acción preparada para seguimiento')}
function renderAdvertising(){return `<div class="intro"><h2>Publicidad</h2><p>Decidí dónde poner dinero según señales de retorno.</p></div><div class="panel"><div class="campaign-grid">${state.analytics.marketing.campaigns.map(c=>campaign(c)).join('')}</div><details class="explain"><summary>Cómo leer esto</summary><p>Una campaña con inversión y 0 ventas debe revisarse antes de escalar. Las campañas con ventas comprobadas tienen evidencia para crecer con cuidado.</p></details></div>`}
function renderCalendar(){return `<div class="intro"><h2>Calendario de marketing</h2><p>Convertí recomendaciones en acciones programadas.</p></div><div class="panel"><div class="calendar-grid simple-calendar">${state.calendar.map(e=>`<div class="day"><strong>${e.date}</strong><span class="event">${e.title}</span><small>${e.type}</small></div>`).join('')}</div><button class="primary-btn" style="margin-top:16px" id="newEvent">+ Agregar acción</button></div>`}
function renderContent(){return `<div class="intro"><h2>Content Studio</h2><p>Creá contenido a partir de lo que ya sabemos que funciona.</p></div><div class="content-cards">${state.content.map(c=>`<div class="content-card"><span class="demo-badge">${c.channel}</span><h3>${c.title}</h3><p class="muted">Alcance ${money(c.reach)} · ${money(c.interactions)} interacciones</p><strong>${c.status}</strong></div>`).join('')}<div class="content-card"><span class="demo-badge">IA + datos</span><h3>Un plato, 3 secretos</h3><p class="muted">Repetir el formato detrás de cocina con CTA para reservar.</p><button class="primary-btn" id="createContent">Crear contenido</button></div></div>`}
function renderCustomers(){const s=state.customers;return `<div class="intro"><h2>Clientes</h2><p>Convertí la base de clientes en oportunidades de fidelización y recuperación.</p></div><div class="segment-grid">${[['Nuevos',s.summary.new],['Recurrentes',s.summary.recurrent],['Inactivos',s.summary.inactive],['Potenciales',s.summary.potential]].map(([l,v])=>`<div class="segment"><span class="muted">${l}</span><strong>${v}</strong></div>`).join('')}</div><div class="table-wrap" style="margin-top:16px"><table class="data-table"><thead><tr><th>Cliente</th><th>Segmento</th><th>Última compra</th><th>Compras</th><th>Valor</th></tr></thead><tbody>${s.customers.map(c=>`<tr><td><b>${c.name}</b></td><td>${c.segment}</td><td>${c.lastPurchase}</td><td>${c.orders}</td><td>$${money(c.value)}</td></tr>`).join('')}</tbody></table></div>`}
function renderWebsite(){return `<div class="intro"><h2>Sitio Web</h2><p>Lectura de conversión del sitio. Datos demo hasta conectar Analytics.</p></div><div class="two-col"><div class="panel">${[['Visitas','12.480','↑ 18%'],['Páginas vistas','25.740','↑ 9%'],['Formularios','184','↑ 15%'],['Contactos','126','↑ 11%']].map(x=>`<div class="metric-line"><b>${x[0]}</b><strong>${x[1]}</strong><span class="trend up">${x[2]}</span></div>`).join('')}</div><div class="panel"><h3>Próxima medición</h3><p>Conectar Google Analytics permitirá reemplazar estos datos demo por tráfico y conversiones reales.</p></div></div>`}
function renderAI(){return `<div class="intro"><h2>Marketing Manager AI</h2><p>Un agente que responde usando el contexto disponible del negocio. No inventa acceso a canales no conectados.</p></div><div class="ai-layout"><section class="chat"><div class="messages" id="messages"><div class="bubble ai">Hola. Soy tu Marketing Manager. Puedo ayudarte con campañas, clientes, contenido, ventas y prioridades.</div></div><form class="chat-form" id="aiForm"><input id="aiQuestion" placeholder="Ej: ¿Qué debería hacer esta semana?" autocomplete="off"/><button class="primary-btn">Preguntar</button></form></section><aside class="ai-side"><h3>Preguntas sugeridas</h3>${['¿Qué campaña funciona mejor?','¿Dónde estoy perdiendo dinero?','¿Qué debería publicar?','¿Cómo recupero clientes?'].map(q=>`<button class="quick-question">${q}</button>`).join('')}</aside></div>`}
function renderIntegrations(){return `<div class="intro"><h2>Integraciones</h2><p>Estado transparente: conectado, no conectado o próximamente.</p></div><div class="integration-grid">${state.integrations.map(i=>`<div class="integration"><div><strong>${i.name}</strong><p class="muted">${i.demo?'Datos demo / simulación':'Preparado para conexión'}</p></div><div><span class="state-pill ${i.state==='Conectado'?'connected':i.state==='Próximamente'?'soon':'not-connected'}">${i.state}</span>${i.state==='No conectado'?`<button class="secondary-btn" style="margin-left:8px" onclick="connectIntegration('${i.id}')">Conectar</button>`:''}</div></div>`).join('')}</div>`}
async function connectIntegration(id){const r=await api(`/api/integrations/${id}/connect`,{method:'POST',body:'{}'});showToast(r.message);loadRoute()}
async function loadRoute(){const r=state.route;setActive(r);title.textContent={dashboard:'Dashboard',analytics:'Analytics',recommendations:'Recomendaciones',advertising:'Publicidad',calendar:'Calendario',content:'Contenido',customers:'Clientes',website:'Sitio Web',ai:'Marketing Manager AI',integrations:'Integraciones'}[r]||'Dashboard';if(r==='dashboard')state.data=await api('/api/dashboard');if(r==='analytics'||r==='advertising')state.analytics=await api('/api/analytics');if(r==='recommendations')state.recommendations=await api('/api/recommendations');if(r==='customers')state.customers=await api('/api/customers');if(r==='content')state.content=await api('/api/content');if(r==='calendar')state.calendar=await api('/api/calendar');if(r==='integrations')state.integrations=await api('/api/integrations');const views={dashboard:renderDashboard,analytics:renderAnalytics,recommendations:renderRecommendations,advertising:renderAdvertising,calendar:renderCalendar,content:renderContent,customers:renderCustomers,website:renderWebsite,ai:renderAI,integrations:renderIntegrations};app.innerHTML=(views[r]||renderDashboard)();if(r==='analytics')renderAnalyticsBody();if(r==='ai')bindAI();if(r==='calendar')document.getElementById('newEvent').onclick=addEvent;if(r==='content')document.getElementById('createContent').onclick=addContent}
async function addEvent(){const eventTitle=prompt('Nombre de la acción');if(!eventTitle)return;const date=prompt('Fecha (YYYY-MM-DD)','2026-09-20');if(!date)return;await api('/api/calendar',{method:'POST',body:JSON.stringify({title:eventTitle,date,type:'Contenido'})});showToast('Acción agregada');loadRoute()}
async function addContent(){const contentTitle=prompt('Idea de contenido','Reel: detrás de cocina #2');if(!contentTitle)return;await api('/api/content',{method:'POST',body:JSON.stringify({title:contentTitle,channel:'Instagram',scheduled:null})});showToast('Contenido creado');loadRoute()}
function bindAI(){const form=document.getElementById('aiForm');form.onsubmit=async e=>{e.preventDefault();const input=document.getElementById('aiQuestion'),q=input.value.trim();if(!q)return;const box=document.getElementById('messages');box.insertAdjacentHTML('beforeend',`<div class="bubble user">${escapeHtml(q)}</div>`);input.value='';const r=await api('/api/ai',{method:'POST',body:JSON.stringify({question:q})});const a=r.answer;box.insertAdjacentHTML('beforeend',`<div class="bubble ai"><b>Dato:</b> ${a.data}<br><b>Análisis:</b> ${a.analysis}<br><b>Conclusión:</b> ${a.conclusion}<br><b>Recomendación:</b> ${a.recommendation}<br><b>Acción:</b> ${a.action}<small class="muted" style="display:block;margin-top:8px">Fuente: ${a.source}</small></div>`)};document.querySelectorAll('.quick-question').forEach(b=>b.onclick=()=>{document.getElementById('aiQuestion').value=b.textContent;form.requestSubmit()})}
const escapeHtml=s=>s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
async function renderRoute(){if(!state.token){showLogin();return}try{await loadRoute()}catch(e){console.error(e)}}
window.executeReco=executeReco;window.connectIntegration=connectIntegration;window.addEventListener('hashchange',()=>{state.route=location.hash.slice(1)||'dashboard';renderRoute()});document.getElementById('mobileMenu').onclick=()=>document.getElementById('sidebar').classList.toggle('open');document.getElementById('refreshBtn').onclick=()=>loadRoute();renderRoute();
